[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "crm-data-simulator"
version = "0.1.0"
dependencies = [
    "fastapi",
    "uvicorn",
    "huggingface_hub",
    "openenv-core>=0.2.0"
]

[project.scripts]
server = "main:app"
